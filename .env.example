# Preferred (Supabase/PostgreSQL)
DATABASE_URL=
DATABASE_SSL=true

# Optional fallback (used if DATABASE_URL is empty)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=
DB_NAME=bloodbank_db
DB_PORT=5432
DB_CONNECTION_LIMIT=10
DB_CONNECT_TIMEOUT_MS=10000
DB_IDLE_TIMEOUT_MS=30000
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX=30
PASSWORD_RESET_TOKEN_TTL_MINUTES=30
EMAIL_VERIFICATION_TOKEN_TTL_HOURS=24
CORS_ORIGIN=
APP_BASE_URL=
DISPOSABLE_EMAIL_DOMAINS=
ADMIN_API_KEY=
REQUIRE_AUTHORITY_FOR_DONATION_COMPLETION=false

# SMTP settings (required for forgot password, email verification, and contact-form emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
CONTACT_RECEIVER_EMAIL=

DONOR_SEARCH_CACHE_TTL_SECONDS=120
REDIS_URL=
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_RETRY_MS=30000
REDIS_CONNECT_TIMEOUT_MS=2000
PORT=3000
